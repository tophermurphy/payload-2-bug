import { Block } from "payload/types";

export const Paragraph: Block = {
  slug: "paragraph",
  fields: [
    {
      name: "paragraph",
      type: "text",
    },
  ],
};

export default Paragraph;
